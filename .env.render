# ============================================
# Render Deployment Configuration
# ============================================
# Configurar estas variables en Render Dashboard
# Settings > Environment

# ---- API Keys (OBLIGATORIO) ----
GEMINI_API_KEY=tu_gemini_api_key_de_produccion

# ---- Database (Automático desde render.yaml) ----
# SPRING_DATASOURCE_URL=postgresql://...  (auto)
# SPRING_DATASOURCE_USERNAME=...          (auto)
# SPRING_DATASOURCE_PASSWORD=...          (auto)

# ---- Application ----
SPRING_PROFILES_ACTIVE=prod
APP_BASE_URL=https://tu-app.onrender.com

# ---- Upload Directory ----
APP_UPLOAD_DIR=/opt/render/project/src/uploads/profile-photos

# ---- Java Options ----
# Free tier: 512MB max
JAVA_OPTS=-Xmx512m -Xms256m -XX:+UseG1GC

# ---- Server ----
SERVER_PORT=8080

# ---- CORS (opcional) ----
CORS_ALLOWED_ORIGINS=https://tu-frontend.com

# ============================================
# NOTAS IMPORTANTES PARA RENDER
# ============================================

# 1. NO incluir credenciales de base de datos aquí
#    Render las inyecta automáticamente desde render.yaml

# 2. Variables sensibles:
#    - Configurar en Dashboard > Settings > Environment
#    - Render las encripta automáticamente

# 3. URL del servicio:
#    - Por defecto: https://[service-name].onrender.com
#    - Puedes usar dominio personalizado

# 4. Almacenamiento (FREE TIER):
#    - NO hay almacenamiento persistente en free tier
#    - Las fotos de perfil se perderán al reiniciar
#    - Soluciones:
#      a) Usar plan pago con Persistent Disk
#      b) Migrar a Cloudinary/S3 (recomendado)

# 5. Auto-scaling:
#    - Free tier: 1 instancia, se suspende tras 15 min inactividad
#    - Paid tier: Auto-scaling disponible
