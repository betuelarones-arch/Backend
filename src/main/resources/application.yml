spring:
  profiles:
    active: dev
  application:
    name: cliente-app
  datasource:
    url: jdbc:postgresql://${DB_HOST:ep-ancient-art-ad4nk014-pooler.c-2.us-east-1.aws.neon.tech}:${DB_PORT:5432}/${DB_NAME:neondb}?sslmode=require&sslfactory=org.postgresql.ssl.NonValidatingFactory&connectTimeout=10&socketTimeout=30
    username: ${DB_USER:neondb_owner}
    password: ${DB_PASSWORD:npg_qiYsTxj5w4pv}
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 10000
      maximum-pool-size: 5
      minimum-idle: 1
      idle-timeout: 30000
      max-lifetime: 180000
      connection-init-sql: SELECT 1
      leak-detection-threshold: 60000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080
  servlet:
    context-path: /api

# Environment variables with default values for local development
DB_HOST: ep-ancient-art-ad4nk014-pooler.c-2.us-east-1.aws.neon.tech
DB_PORT: 5432
DB_NAME: neondb
DB_USER: neondb_owner
DB_PASSWORD: npg_qiYsTxj5w4pv

# Logging configuration
logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    com.zaxxer.hikari: DEBUG