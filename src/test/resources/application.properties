spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;INIT=CREATE SCHEMA IF NOT EXISTS CORE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
# Provide defaults for external placeholders used by some beans
DJANGO_API_BASE=http://localhost/django
# Test-only default for OpenAI API key used by some services
openai.api.key=TEST_OPENAI_KEY
